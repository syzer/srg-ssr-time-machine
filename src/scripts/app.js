var SSR = {
    endpoints: {
        wit: 'bla',
        ssr: {
            schedules: 'http://api.programmes.rts.ch:80/api/schedules/day/'
        }
    }
};

$(document).foundation();

$(document).ready(function() {
    $(".fancybox").fancybox();
});


